# universal-storage-connector
A vendor-agnostic Java library that abstracts cloud object storage operations behind a single, consistent API. Supports multiple providers including AWS S3 and Azure Blob Storage, enabling seamless multi-cloud file uploads without vendor lock-in.
